#include <iostream>
#define INF -6
#define SUP 6
#define LENGTH 3
int quadratic (int);
using namespace std;
int poly[LENGTH] = {1, -2, -3};
int derivpoly[LENGTH] = {0, 2, -2};

int main (){

	int p;

	do{
		cout << "Ingrese el parámetro de la función cuadrática"; cin >> p;
	} while (p >= INF && p <= SUP);

	quadratic(p);


	return 0;
}

int quadratic (int p){

	int minimum
	int fp[LENGTH];
	for(int i = 0; i < LENGTH; i++){
		fp[i] = 1 * poly[i] * 

	}

	return minimum;
}